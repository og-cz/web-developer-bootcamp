// spread with array just concatinates each the array you inserted
const cats = ['blue', 'scout', 'rockets'];
const dogs = ['rusty', 'john', 'howards'];

const allPets = [...cats, ...dogs] // concat array using this
console.log(allPets);
